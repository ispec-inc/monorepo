package {{ .PackageName }}

import "github.com/graphql-go/graphql"

type TypeRegistry interface {
    {{- range $g := .Generators }}
    {{- if not .IsDefinedType }}{{- if not .IsPrivate }}
    {{ .Definition.Name }}() graphql.Type{{- end }}{{- end }}
    {{- end }}
}
