type Mutation {
    createArticle(input: ArticleInput!): Article
    createHero(input: HeroInput!): Hero
}

input ArticleInput {
    title: String!
    body: String!
}

input HeroInput {
    name: String!
    age: Int!
    isJedi: Boolean
}
